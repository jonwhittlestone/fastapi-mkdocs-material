{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "⚡ FastAPI",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/.venv/bin/uvicorn",
      "console": "integratedTerminal",
      "justMyCode": false,
      "args": ["src.main:app", "--port", "8532", "--reload"],
      "jinja": true
    }
  ]
}
